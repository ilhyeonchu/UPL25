(include_subdirs unqualified)


;(executable
;  (name helloworld))

(library
  (name foo_test)
  (inline_tests)
  (preprocess (pps ppx_inline_test)))
  ;(name foo)
  ;(modules foo))

;(executable
  ;(name tes))
